import "package:flutter/material.dart";

class App extends StatelessWidget{
Widget build(context){
  return MaterialApp(
    title: 'News',
    home: Scaffold(
      body: Text('Just text'),
    ),
  );
}
}